# Reflection

## 1. มี​เทสต์​ทั้งหมด​กี่​ตัว​และ​โค้ด​จริง​กี่​บรรทัด​อัตราส่วน​เป็น​เท่าใด

- มีเทสต์ 6 ตัว 
- โค้ดจริงใน kitchen.py มี 22 บรรทัด 
- คิดเป็นเทสต์ 1 ตัวต่อโค้ดราว 3.7 บรรทัด ถ้านับเป็นบรรทัด โค้ดเทสต์กับโค้ดจริงเกือบเท่ากันพอดี ประมาณ 1.1 : 1

## 2. ช่วง​ที่​แถบ​เป็น​สีแดง​นาน​ที่สุด​คือ​เมื่อใด​อะไร​ทำให้​มัน​นาน

ตอน A6 ที่เขียน `test_simple_addition` เพราะเทสต์ตัวเดียวดันต้องการของใหม่ถึง 3 อย่างพร้อมกัน `plus`, `Sum`, `Converter` จะทำให้เขียวทีละนิดไม่ได้ ต้องคิดก่อนว่า `plus()` ควรคืนอะไรถึงจะรองรับเคสหน่วยผสมที่รออยู่ในลิสต์ได้ เลยแดงค้างนานกว่าทุก Task สรุปได้ว่า  Task ที่ต้องตัดสินใจเรื่องดีไซน์กินเวลากว่า Task ที่แค่เพิ่มพฤติกรรมใหม่

## 3. หา​จุด​ที่​ใช้ Fake It หนึ่ง​จุด​ใช้ Triangulate หนึ่ง​จุด​และ​ใช้ Obvious Implementation หนึ่ง​จุด​เมื่อ​มอง​ย้อนกลับ​ไป​แต่ละครั้ง​เลือก​ได้​เหมาะสม​หรือไม่

- Fake It คือ `times()` ตอนแรกเขียน `self.amount = 600` ไปเฉยๆ
- Triangulate คือการเพิ่มเทสต์ 200×2 = 400 มาเพื่อเลิก hardcode
- Obvious คือ `__eq__` ที่เขียนของจริงลงไป สองอันแรกเหมาะกับตอนฝึกจังหวะ
- ถ้าทำอีกรอบคงเขียนคูณตรงๆ เพราะมันง่ายกว่าการ fake ส่วน `__eq__` ได้บทเรียนว่าอะไรที่คิดว่า obvious ก็พลาดได้ เวอร์ชันแรกเทียบแค่ amount เลยปล่อยให้ 1 g เท่ากับ 1 oz อยู่พักหนึ่งจนเทสต์ใน A5 มาจับได้

## 4. ยังมี​รายการ​ใด​ค้าง​อยู่​ใน​รายการ​ทดสอบ​รายการ​ใด​ที่จะ​ลงมือทำ​จริง​และ​รายการ​ใด​ที่​ควร​ลบทิ้ง​เพราะ​เป็นการ​คาดเดา​ล่วงหน้า

ค้างอยู่สองรายการท้าย ที่จะทำจริงคือ 200 g + 1 oz เพราะเป็นเหตุผลที่มี Converter ตั้งแต่แรกแต่ตอนนี้ยังรองรับไม่ได้ ส่วน (200 g + 1 oz) × 2 ควรลบทิ้ง เพราะยังไม่มีที่ใช้จริง เป็นการคาดเดาล่วงหน้า

## 5. ดู​เมท็อด Quantity.reduce ถ้า​ออกแบบ​ระบบ​ทั้งหมด​บน​กระดาษ​ก่อน​นักศึกษา​จะ​เขียน​เมท็อด​นี้​ออกมา​หรือไม่

ไม่น่าจะเขียนออกมา ถ้าออกแบบบนกระดาษคงได้ `add()` ที่บวกแล้วคืนค่าทันที ไม่ใช่ `plus()` ที่คืน Sum แล้วค่อย `reduce()` ทีหลัง ดีไซน์นี้เกิดจากเคส 200 g + 1 oz ในลิสต์ที่บวกทันทีไม่ได้ แถม reduce ตอนนี้ก็ยังไม่เสร็จ ถ้าส่งหน่วยผสมเข้าไปจะได้ 201 g ทำให้เห็นว่าโค้ดโตจากการ TDD
